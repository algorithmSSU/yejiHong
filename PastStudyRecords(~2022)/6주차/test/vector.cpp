#include <iostream>
#include <vector>
using namespace std;

int main(){

    vector<vector<int>> a(10);

    int n;
    for (int i = 0; i < n;i++){}

        cout << a.size();

    
    for (int i = 0; i < a.size();i++){
        for (int j = 0; j < a[i].size();j++){
            cout << a[i][j] << " ";
        }
        cout << "\n";
    }
    
};